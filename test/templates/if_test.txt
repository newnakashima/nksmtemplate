これはテストです。
{{if hoge}}
    hogeのときだけここが出力されます。
    {{if nest1}}
        nest1のときだけここが出力されます。
        {{if nest2}}
            nest2のときだけここが出力されます。
        {{fi}}
    {{fi}}
{{fi}}
ここは共通で出力されます。
hogeは{{ hoge }}でした。
nest1は{{ nest1 }}でした。
nest2は{{ nest2 }}でした。
